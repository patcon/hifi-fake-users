name: "HiFi: Fake User"
on:
  push:
  repository_dispatch:
    types:
      - fake-user
jobs:
  tests:
    runs-on: ubuntu-latest
    env:
      CYPRESS_BASE_URL: https://map.highfidelity.com
      HIFI_SPACE: ${{ github.event.client_payload }}
    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Chrome
        uses: cypress-io/github-action@v1
        with:
          browser: chrome

      - uses: actions/upload-artifact@v1
        with:
          name: videos
          path: cypress/videos
